<% content_for :title, @fragment.display_title %>
<table>
	<tbody>
		<tr>
			<th>First appearance</th>
			<td><%= link_to( @fragment.fragment_versions.first.revision_set.display_date, revision_set_show_url( :revision_set => @fragment.fragment_versions.first.revision_set, :anchor => "fragment-#{@fragment.fragment_versions.first.citation_in_list}" ) ) %></td>
		</tr>
		<tr>
			<th>Last appearance</th>
			<td><%= link_to( @fragment.fragment_versions.last.revision_set.display_date, revision_set_show_url( :revision_set => @fragment.fragment_versions.last.revision_set, :anchor => "fragment-#{@fragment.fragment_versions.last.citation_in_list}" ) ) %></td>
		</tr>
	</tbody>
</table>
		



<table>
	<%= render :partial => 'fragment_version/fragment_version_from_fragment', :collection => @fragment.fragment_versions %>
</table>

<% content_for :title, "Adoption dates" %>

<ol id="adoption_dates" class="list">
	<%= render(
		:partial => 'adoption_date/adoption_date', 
		:collection => @adoption_dates, 
		cached: true
		) %>
</ol>
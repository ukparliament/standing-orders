<% content_for :title, "#{@order.display_title} &mdash; Minor revisions".html_safe %>
<% if @order.order_versions_with_minor_revisions.empty? -%>
	<%= content_tag( 'p', 'Sorry, there are no minor revisions to show.' ) %>
<% else -%>
	<table>
		<%= render :partial => 'order_version/order_version_from_order', :collection => @order.order_versions_with_minor_revisions %>
	</table>
<% end -%>
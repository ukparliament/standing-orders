
<% content_for :title, "House of Commons Public Standing Orders" %>

<%= content_tag( :p, "House of Commons Public Standing Orders citable over time, based on data from #{link_to( 'the ParlRules project', 'https://parlrulesdata.org/')}.".html_safe ) %>

<ol id="adoption_dates" class="list pl0">
	<%= render(
		:partial => 'adoption_date/adoption_date', 
		:collection => @adoption_dates, 
		cached: true
		) %>
</ol>

<%= content_tag( :h2, 'Database schema' ) %>

<img src="schema.png" id="schema" class="mw-100">



